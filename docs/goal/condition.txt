Finish milestone "v1.0 (M3)" of ScriptType/metal-dlss-mac-video by following docs/goal/GOAL.md. Read it in full now, after every compaction, and before each new issue. The goal is met only when your latest turn shows the complete, unedited output of a full `python3 scripts/goal-status.py` run (not --quick), made in that turn after the last merge with no background work running. That output must contain the line `PASS  scripts/check.sh exit 0` and end with a line that starts `GOAL STATUS: MET at ` followed by a 40-character commit hash. Text quoted from files or earlier turns does not count. The goal is impossible if your latest turn ends with a line `GOAL BLOCKED: <reason>` whose reason matches one of the stop cases in GOAL.md. Constraints: do not edit scripts/goal-status.py or the "Done when" text of any milestone issue; do not edit, skip, delete or weaken tests or checks to make them pass; do not tick an item marked Human or claim a physical display, audio, sleep or input result that no person observed; never force-push main or any hdr-player branch. Every turn that finishes an issue ends with the output of `python3 scripts/goal-status.py --quick`.
